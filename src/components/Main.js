import React from "react"
import TableData from "./main/TableData"

const Main = (props) =>{
    return(
        <main className={"Main"}>
            <TableData/>
        </main>
    )
}

export default Main